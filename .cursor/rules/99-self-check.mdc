---
description: AiHub 自检与纠正 — 每次实质性改动前后必做
alwaysApply: true
---

# Self-Check & Correction

## 自检（动手前 / 交付前各过一遍）

1. **目标对齐**：当前改动是否直接对应用户请求，有无范围蔓延？
2. **约束合规**：是否违反 `11-project-arch` / `CLAUDE.md`（目录、前后端边界、SQLite、串行评测、部署）？
3. **不确定性**：是否仍有未确认的领域/契约假设？有则应先问停。
4. **验证路径**：能否用 `make verify`（或相关 target）证明未破坏关键路径？
5. **副作用**：是否影响 DB、部署脚本、API 契约、评测行为？
6. **可逆与记录**：重要决策是否已写入或应写入 `DECISIONS.md`？

## 纠正行为（自检失败时）

1. **停**：发现越界或未确认假设 → 立即停止继续堆代码。
2. **问**：把不确定点整理成具体问题，向用户确认。
3. **收**：撤回或缩小无关改动，只保留必要 diff。
4. **修**：按已确认约束修正违规（例如前端直连 DB → 改为走 API）。
5. **验**：再跑 `make lint` / `make typecheck` / `make test-critical`（视改动选择）；失败则启用 self-heal（最多 3 轮）。
6. **记**：若纠正涉及架构或契约，追加 `DECISIONS.md` 条目。
